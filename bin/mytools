#!/usr/bin/env python3
"""Executable wrapper. The implementation lives in the mytools package."""

import os
import sys

sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from mytools.cli import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
